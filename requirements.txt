# IDX Stock Scanner — Streamlit Community Cloud requirements
# ─────────────────────────────────────────────────────────────────────────────
# HANYA berisi dependency yang benar-benar dibutuhkan oleh dashboard runtime.
# Scanner pipeline (yfinance, ta, xgboost, scikit-learn) diinstall terpisah
# di lokal dengan: pip install -e ".[scanner]"
#
# Streamlit Cloud otomatis juga menjalankan `pip install .` karena ada
# pyproject.toml — dependency [project.dependencies] sudah dibuat minimal
# (pandas, numpy, pyarrow, loguru, pyyaml, requests) agar tidak bentrok.
# ─────────────────────────────────────────────────────────────────────────────

# Dashboard
# PIN streamlit: the unpinned ">=1.35,<2.0" let Streamlit Cloud install the
# latest 1.x (e.g. 1.58), which (a) changes native-widget DOM/CSS so theme
# overrides can miss, and (b) breaks this app's selectbox session_state usage
# (KeyError on widget keys). 1.37.1 is the version the dashboard is built and
# contrast-verified against, so pin it for a deterministic, working deploy.
#
# DEPLOY PYTHON VERSION — IMPORTANT: use Python 3.12 (or 3.11/3.13) on Streamlit
# Community Cloud. streamlit 1.37.1 requires `pillow<11`, and the newest such
# Pillow (10.4.0) ships wheels only up to cp313 — it has NO cp314 wheel. If the
# deploy runs on Python 3.14 (Cloud's current forced default), pip can't find a
# Pillow wheel and tries to build it from source -> "Failed building wheel for
# pillow". On Python 3.12 every dependency installs from a wheel. Set the Python
# version in the app's deploy "Advanced settings" (see runtime.txt / .python-version).
streamlit==1.37.1
plotly>=5.22

# Data
pandas>=2.1
numpy>=1.26
pyarrow>=15.0

# Config & logging
pyyaml>=6.0
loguru>=0.7

# HTTP (remote payload fetch dari GitHub raw URL)
requests>=2.31

# Multi-period financial statements (long-term detail view does a lazy
# `import yfinance`). Required at runtime by the deployed dashboard.
yfinance>=0.2.40

# Reading/serving the daily Signal Performance .xlsx export.
openpyxl>=3.1
